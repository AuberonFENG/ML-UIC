Here are two code file of Alexnet with different method:
"AlexNet.ipynb" is the model we implement with keras and simplify the parameter of every layer
"AlexNet_test.py" is the model we implement every layer in class